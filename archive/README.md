# Archive

Archived modules from earlier Mapanare versions. Preserved for reference only.

## stdlib/

Python stdlib modules deprecated in v2.0.0. Replaced by native `.mn` stdlib modules
compiled via LLVM. See `stdlib/` in the project root for current modules.
