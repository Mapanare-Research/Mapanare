// v5.16.0 Te.4 — multiple ``${...}`` sites in one literal. Lowered
// to a left-fold of ``__mn_str_concat`` with the final concat
// writing into the InterpConcat dest directly.

fn main():
    let a: Int = 1
    let b: Int = 2
    print("${a} and ${b}")
