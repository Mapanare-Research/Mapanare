// Golden test: FFI binding target — public API functions.

fn add(a: Int, b: Int) -> Int:
    return a + b

fn factorial(n: Int) -> Int:
    if n <= 1 { return 1 }
    return n * factorial(n - 1)

fn main():
    print(add(3, 4))
    print(factorial(5))
