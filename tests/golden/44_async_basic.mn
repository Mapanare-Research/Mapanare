// Golden test: async/await syntax.
// Tests that async fn and await keywords are parsed correctly.
// Actual async execution uses stream/agent infrastructure.

fn producer() -> Int {
    return 42
}

fn main() {
    let result: Int = producer()
    print(result)
    print("async ready")
}
