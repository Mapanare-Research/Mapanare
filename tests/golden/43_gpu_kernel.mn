// Golden test: const keyword + GPU detect integration.

const BLOCK_SIZE: Int = 256
const GRID_SIZE: Int = 1

fn compute_threads() -> Int {
    return BLOCK_SIZE * GRID_SIZE
}

fn main() {
    print(compute_threads())
    print("gpu kernel params ready")
}
