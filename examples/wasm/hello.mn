// Hello World — WebAssembly
// Compile: mapanare emit-wasm examples/wasm/hello.mn
// Run: load in browser via playground

fn main() {
    print("Hello from Mapanare on WASM!")
    print("The AI-native language, running in the browser.")
    let x: Int = 6 * 7
    print(str(x))
}
