Mapanare agents are first-class concurrent primitives. An agent has typed input and output channels. You define an agent with the `agent` keyword, spawn it with `spawn`, send messages with `<-`, and receive with `sync`. Agents run concurrently via a cooperative scheduler on mobile or OS threads on desktop. Each agent has an inbox (SPSC ring buffer) and processes messages one at a time.
