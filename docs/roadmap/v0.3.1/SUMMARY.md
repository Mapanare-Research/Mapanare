# v0.3.1 Summary — "Release Polish"

**Released:** March 2026
**Theme:** Versioning and documentation cleanup

---

## What shipped

- **Dynamic versioning:** `VERSION` file is the single source of truth; `pyproject.toml` and `__version__` read from it dynamically
- **Documentation tests:** verified doc examples compile and run correctly

## Scope

This was a patch release to clean up version management after v0.3.0. No new features or language changes.
