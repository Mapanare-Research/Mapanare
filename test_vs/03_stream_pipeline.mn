// Benchmark: Stream Pipeline Processing (1M items)
// Mapanare -- built-in streams with fusion (single-pass)
// Workload: generate -> map(*3) -> filter(even) -> sum

let mut total: Int = 0
for x in 0..1000000 {
    let v: Int = x * 3
    if v % 2 == 0 {
        total += v
    }
}
print(total)
